# SprintCheck — Landing Page

A pixel-faithful, fully responsive implementation of the new version of the  **SprintCheck** Landing 
page, built with the **Next.js App Router**, **TypeScript** and
**Tailwind CSS**.


## Getting started

```bash
npm install
npm run dev      # http://localhost:3000
npm run build    # production build
npm start        # serve the production build
```

#